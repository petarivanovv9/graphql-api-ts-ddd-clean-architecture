export class AccountService {
  async getAccountByCompany(company: string) {
    // TODO:
    // >>> IMPLEMENT

    console.log('calling `getAccountByCompany` with `company` ', company);

    return Promise.resolve(undefined);
  }
}
